const numbers = [1, 2, 3, 4, 5];
for(const number of numbers){
    // console.log(number);
}

// numbers.map(num => {
//     console.log(num);
// })

const employee = {
    name: 'Mufasa',
    age: 55,
    kingdom: 'Pride Lands',
    position: 'Softwore Engineer'
}

for(const emp in employee){
    // console.log(employee[emp]);
}